<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Upload</title>
  </head>
  <body>
    <a href="/logout">Logout</a>

    <% if (locals.user) { %>
    <h1>Welcome <%- user.fullName %></h1>
    <% } %>

    <div>Show upload stuff here</div>
  </body>
</html>
